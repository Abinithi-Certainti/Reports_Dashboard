# Report files and exports carry real company data; keep them out of the repo
*.pbix
*.abf
*.pdf
*.png
# Secrets
.env
.env.*
# Build output and installed packages (regenerated by mvn / npm)
engine/backend/target/
engine/frontend/node_modules/
engine/frontend/dist/
engine/frontend/*.tsbuildinfo
*.webm
*.mp4
engine/backend/imported-reports/
